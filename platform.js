(function(root){'use strict';function detectPlatform(ua,platform,touch=0){if(/Android/i.test(ua))return'android';if(/iPad|iPhone|iPod/i.test(ua)||(/Mac/i.test(platform)&&touch>1))return'unknown';if(/Windows/i.test(ua)||/Win/i.test(platform))return'windows';if(/Mac/i.test(ua+' '+platform))return'mac';if(/Linux/i.test(ua+' '+platform))return'linux';return'unknown';}if(typeof module!=='undefined')module.exports={detectPlatform};if(typeof document==='undefined')return;const buttons=[...document.querySelectorAll('[data-platform]')],cards=[...document.querySelectorAll('[data-download]')];function select(key){buttons.forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.platform===key)));cards.forEach(c=>{c.hidden=key!=='all'&&c.dataset.download!==key;});}buttons.forEach(b=>b.addEventListener('click',()=>select(b.dataset.platform)));const detected=detectPlatform(navigator.userAgent,navigator.platform,navigator.maxTouchPoints);const note=document.getElementById('platform-note');if(note)note.textContent=detected==='unknown'?'Choose your platform below. All downloads remain available.':`Suggested for ${detected==='mac'?'macOS':detected}. You can choose another platform below.`;select(detected==='unknown'?'all':detected);})(typeof globalThis==='undefined'?this:globalThis);
